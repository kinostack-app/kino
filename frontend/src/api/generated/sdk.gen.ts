// This file is auto-generated by @hey-api/openapi-ts

import { type Client, formDataBodySerializer, type Options as Options2, type TDataShape } from './client';
import { client } from './client.gen';
import type { AcquireEpisodeByTmdbData, AcquireEpisodeByTmdbErrors, AcquireEpisodeByTmdbResponses, AcquireEpisodeData, AcquireEpisodeErrors, AcquireEpisodeResponses, AddDeviceData, AddDeviceErrors, AddDeviceResponses, AnalyseSeasonIntroData, AnalyseSeasonIntroErrors, AnalyseSeasonIntroResponses, BeginDeviceData, BeginDeviceErrors, BeginDeviceResponses, BlocklistAndSearchData, BlocklistAndSearchErrors, BlocklistAndSearchResponses, BootstrapData, BootstrapResponses, BrowseData, BrowseErrors, BrowseResponses, CalendarData, CalendarIcsData, CalendarIcsResponses, CalendarResponses, CancelDownloadData, CancelDownloadErrors, CancelDownloadResponses, CancelFfmpegDownloadData, CancelFfmpegDownloadResponses, ClearMovieBlocklistData, ClearMovieBlocklistResponses, CreateBackupData, CreateBackupResponses, CreateBootstrapTokenData, CreateBootstrapTokenResponses, CreateCliTokenData, CreateCliTokenResponses, CreateIndexerData, CreateIndexerResponses, CreateListData, CreateListErrors, CreateListResponses, CreateMovieData, CreateMovieErrors, CreateMovieResponses, CreateQualityProfileData, CreateQualityProfileResponses, CreateSessionData, CreateSessionErrors, CreateSessionResponses, CreateShowData, CreateShowErrors, CreateShowResponses, CreateWebhookData, CreateWebhookResponses, DeleteBackupData, DeleteBackupErrors, DeleteBackupResponses, DeleteBlocklistData, DeleteBlocklistErrors, DeleteBlocklistResponses, DeleteDeviceData, DeleteDeviceErrors, DeleteDeviceResponses, DeleteIndexerData, DeleteIndexerErrors, DeleteIndexerResponses, DeleteListData, DeleteListErrors, DeleteListResponses, DeleteMediaData, DeleteMediaErrors, DeleteMediaResponses, DeleteMovieData, DeleteMovieErrors, DeleteMovieResponses, DeleteQualityProfileData, DeleteQualityProfileErrors, DeleteQualityProfileResponses, DeleteShowData, DeleteShowErrors, DeleteShowResponses, DeleteWebhookData, DeleteWebhookErrors, DeleteWebhookResponses, DirectData, DirectErrors, DirectResponses, DiscardEpisodeData, DiscardEpisodeErrors, DiscardEpisodeResponses, DisconnectData, DisconnectErrors, DisconnectResponses, DiscoverMoviesData, DiscoverMoviesResponses, DiscoverShowsData, DiscoverShowsResponses, DownloadBackupData, DownloadBackupErrors, DownloadBackupResponses, DownloadFilesData, DownloadFilesErrors, DownloadFilesResponses, DownloadPeersData, DownloadPeersErrors, DownloadPeersResponses, DownloadPiecesData, DownloadPiecesErrors, DownloadPiecesResponses, DryRunData, DryRunErrors, DryRunResponses, EpisodeReleasesData, EpisodeReleasesErrors, EpisodeReleasesResponses, ExportBundleData, ExportBundleResponses, ExportLogsData, ExportLogsResponses, GenresData, GenresResponses, GetConfigData, GetConfigErrors, GetConfigResponses, GetDefinitionData, GetDefinitionErrors, GetDefinitionResponses, GetDownloadData, GetDownloadErrors, GetDownloadResponses, GetFfmpegDownloadData, GetFfmpegDownloadResponses, GetHealthData, GetHealthResponses, GetHomePreferencesData, GetHomePreferencesResponses, GetImageData, GetImageErrors, GetImageResponses, GetIndexerData, GetIndexerErrors, GetIndexerResponses, GetListData, GetListErrors, GetListResponses, GetMediaData, GetMediaErrors, GetMediaResponses, GetMediaStreamsData, GetMediaStreamsErrors, GetMediaStreamsResponses, GetMovieBlocklistData, GetMovieBlocklistResponses, GetMovieData, GetMovieErrors, GetMovieResponses, GetQualityProfileData, GetQualityProfileErrors, GetQualityProfileResponses, GetRefreshStateData, GetRefreshStateResponses, GetSessionData, GetSessionErrors, GetSessionResponses, GetShowData, GetShowErrors, GetShowResponses, GetStatus2Data, GetStatus2Responses, GetStatusData, GetStatusResponses, GrabAndWatchData, GrabAndWatchErrors, GrabAndWatchResponses, GrabReleaseData, GrabReleaseErrors, GrabReleaseResponses, HlsMasterData, HlsMasterErrors, HlsMasterResponses, HlsSegmentData, HlsSegmentErrors, HlsSegmentResponses, HlsVariantData, HlsVariantErrors, HlsVariantResponses, IgnoreItemData, IgnoreItemErrors, IgnoreItemResponses, ImportData, ImportErrors, ImportResponses, IngestClientLogsData, IngestClientLogsErrors, IngestClientLogsResponses, IssueCastTokenData, IssueCastTokenErrors, IssueCastTokenResponses, LanProbeData, LanProbeResponses, LibrarySearchData, LibrarySearchResponses, ListBackupsData, ListBackupsResponses, ListBlocklistData, ListBlocklistResponses, ListDefinitionsData, ListDefinitionsResponses, ListDevicesData, ListDevicesResponses, ListDownloadsData, ListDownloadsResponses, ListEpisodesData, ListEpisodesResponses, ListHistoryData, ListHistoryResponses, ListIndexersData, ListIndexersResponses, ListItemsData, ListItemsErrors, ListItemsResponses, ListListsData, ListListsResponses, ListLogsData, ListLogsResponses, ListMediaData, ListMediaResponses, ListMoviesData, ListMoviesResponses, ListQualityProfilesData, ListQualityProfilesResponses, ListReleasesData, ListReleasesResponses, ListSeasonsData, ListSeasonsResponses, ListSessionsData, ListSessionsResponses, ListShowsData, ListShowsResponses, ListTasksData, ListTasksResponses, ListWebhooksData, ListWebhooksResponses, LogoutData, LogoutResponses, MarkEpisodeWatchedData, MarkEpisodeWatchedErrors, MarkEpisodeWatchedResponses, MarkMovieWatchedData, MarkMovieWatchedErrors, MarkMovieWatchedResponses, MdnsTestData, MdnsTestResponses, MkdirData, MkdirErrors, MkdirResponses, MonitoredSeasonsData, MonitoredSeasonsErrors, MonitoredSeasonsResponses, MountsData, MountsResponses, MovieDetailsData, MovieDetailsResponses, MovieReleasesData, MovieReleasesErrors, MovieReleasesResponses, PauseData, PauseDownloadData, PauseDownloadErrors, PauseDownloadResponses, PauseErrors, PauseResponses, PauseShowDownloadsData, PauseShowDownloadsErrors, PauseShowDownloadsResponses, PlacesData, PlacesResponses, PlayData, PlayErrors, PlayProgressData, PlayProgressErrors, PlayProgressResponses, PlayResponses, PollDeviceData, PollDeviceResponses, PrepareData, PrepareErrors, PrepareResponses, ProbeData, ProbeResponses, RateData, RateErrors, RateResponses, RecommendationsData, RecommendationsResponses, RedeemData, RedeemErrors, RedeemResponses, RedownloadEpisodeData, RedownloadEpisodeErrors, RedownloadEpisodeResponses, RefreshDefinitionsData, RefreshDefinitionsErrors, RefreshDefinitionsResponses, RefreshListData, RefreshListErrors, RefreshListResponses, ResetHomePreferencesData, ResetHomePreferencesResponses, RestoreBackupData, RestoreBackupErrors, RestoreBackupResponses, RestoreUploadData, RestoreUploadErrors, RestoreUploadResponses, ResumeDownloadData, ResumeDownloadErrors, ResumeDownloadResponses, ResumeShowDownloadsData, ResumeShowDownloadsErrors, ResumeShowDownloadsResponses, RetryDownloadData, RetryDownloadErrors, RetryDownloadResponses, RetryIndexerData, RetryIndexerErrors, RetryIndexerResponses, RevertFfmpegToSystemData, RevertFfmpegToSystemResponses, RevokeAllData, RevokeAllErrors, RevokeAllResponses, RevokeSessionData, RevokeSessionErrors, RevokeSessionResponses, RotateApiKeyData, RotateApiKeyErrors, RotateApiKeyResponses, RunTaskData, RunTaskErrors, RunTaskResponses, SearchData, SearchResponses, SeasonDetailsData, SeasonDetailsResponses, SeekData, SeekErrors, SeekResponses, SetDefaultQualityProfileData, SetDefaultQualityProfileErrors, SetDefaultQualityProfileResponses, ShowDetailsData, ShowDetailsResponses, ShowSeasonEpisodesByTmdbData, ShowSeasonEpisodesByTmdbResponses, ShowWatchStateData, ShowWatchStateResponses, SignUrlData, SignUrlResponses, SpeedTestData, SpeedTestResponses, StartFfmpegDownloadData, StartFfmpegDownloadErrors, StartFfmpegDownloadResponses, StartSessionData, StartSessionErrors, StartSessionResponses, StatsData, StatsResponses, StatusData, StatusResponses, StopSessionData, StopSessionErrors, StopSessionResponses, StopTranscodeData, StopTranscodeResponses, StopTranscodeSessionData, StopTranscodeSessionResponses, SubtitleData, SubtitleErrors, SubtitleResponses, SyncNowData, SyncNowErrors, SyncNowResponses, TestConnectionData, TestConnectionResponses, TestIndexerData, TestIndexerErrors, TestIndexerResponses, TestOpensubtitlesData, TestOpensubtitlesResponses, TestPathData, TestPathResponses, TestTmdbData, TestTmdbResponses, TestTranscodeData, TestTranscodeResponses, TestWebhookData, TestWebhookErrors, TestWebhookResponses, TranscodeSessionsData, TranscodeSessionsResponses, TranscodeStatsData, TranscodeStatsResponses, TrendingData, TrendingMoviesData, TrendingMoviesResponses, TrendingResponses, TrendingShowsData, TrendingShowsResponses, TrickplaySpriteData, TrickplaySpriteErrors, TrickplaySpriteResponses, TrickplayVttData, TrickplayVttErrors, TrickplayVttResponses, UnmarkEpisodeWatchedData, UnmarkEpisodeWatchedErrors, UnmarkEpisodeWatchedResponses, UnmarkMovieWatchedData, UnmarkMovieWatchedErrors, UnmarkMovieWatchedResponses, UpdateConfigData, UpdateConfigErrors, UpdateConfigResponses, UpdateDownloadFilesData, UpdateDownloadFilesErrors, UpdateDownloadFilesResponses, UpdateHomePreferencesData, UpdateHomePreferencesResponses, UpdateIndexerData, UpdateIndexerErrors, UpdateIndexerResponses, UpdateQualityProfileData, UpdateQualityProfileErrors, UpdateQualityProfileResponses, UpdateShowMonitorData, UpdateShowMonitorErrors, UpdateShowMonitorResponses, UpdateWebhookData, UpdateWebhookErrors, UpdateWebhookResponses, UpNextData, UpNextResponses, WatchNowData, WatchNowErrors, WatchNowResponses, WidgetData, WidgetResponses, WsHandlerData } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * `GET /api/v1/backups`
 */
export const listBackups = <ThrowOnError extends boolean = false>(options?: Options<ListBackupsData, ThrowOnError>) => (options?.client ?? client).get<ListBackupsResponses, unknown, ThrowOnError>({ url: '/api/v1/backups', ...options });

/**
 * `POST /api/v1/backups` — manual create.
 */
export const createBackup = <ThrowOnError extends boolean = false>(options?: Options<CreateBackupData, ThrowOnError>) => (options?.client ?? client).post<CreateBackupResponses, unknown, ThrowOnError>({ url: '/api/v1/backups', ...options });

/**
 * `POST /api/v1/backups/restore-upload` — multipart upload for a
 * backup made on another machine. Single field, name `archive`.
 */
export const restoreUpload = <ThrowOnError extends boolean = false>(options: Options<RestoreUploadData, ThrowOnError>) => (options.client ?? client).post<RestoreUploadResponses, RestoreUploadErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/api/v1/backups/restore-upload',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * `DELETE /api/v1/backups/{id}`
 */
export const deleteBackup = <ThrowOnError extends boolean = false>(options: Options<DeleteBackupData, ThrowOnError>) => (options.client ?? client).delete<DeleteBackupResponses, DeleteBackupErrors, ThrowOnError>({ url: '/api/v1/backups/{id}', ...options });

/**
 * `GET /api/v1/backups/{id}/download` — stream the archive file.
 */
export const downloadBackup = <ThrowOnError extends boolean = false>(options: Options<DownloadBackupData, ThrowOnError>) => (options.client ?? client).get<DownloadBackupResponses, DownloadBackupErrors, ThrowOnError>({ url: '/api/v1/backups/{id}/download', ...options });

/**
 * `POST /api/v1/backups/{id}/restore` — restore from a known row.
 * Auto-creates a pre-restore snapshot first.
 */
export const restoreBackup = <ThrowOnError extends boolean = false>(options: Options<RestoreBackupData, ThrowOnError>) => (options.client ?? client).post<RestoreBackupResponses, RestoreBackupErrors, ThrowOnError>({ url: '/api/v1/backups/{id}/restore', ...options });

/**
 * List blocklist entries (paginated).
 */
export const listBlocklist = <ThrowOnError extends boolean = false>(options?: Options<ListBlocklistData, ThrowOnError>) => (options?.client ?? client).get<ListBlocklistResponses, unknown, ThrowOnError>({ url: '/api/v1/blocklist', ...options });

/**
 * Clear all blocklist entries for a movie.
 */
export const clearMovieBlocklist = <ThrowOnError extends boolean = false>(options: Options<ClearMovieBlocklistData, ThrowOnError>) => (options.client ?? client).delete<ClearMovieBlocklistResponses, unknown, ThrowOnError>({ url: '/api/v1/blocklist/movie/{movie_id}', ...options });

/**
 * Count blocklist entries for a movie.
 */
export const getMovieBlocklist = <ThrowOnError extends boolean = false>(options: Options<GetMovieBlocklistData, ThrowOnError>) => (options.client ?? client).get<GetMovieBlocklistResponses, unknown, ThrowOnError>({ url: '/api/v1/blocklist/movie/{movie_id}', ...options });

/**
 * Delete a blocklist entry.
 */
export const deleteBlocklist = <ThrowOnError extends boolean = false>(options: Options<DeleteBlocklistData, ThrowOnError>) => (options.client ?? client).delete<DeleteBlocklistResponses, DeleteBlocklistErrors, ThrowOnError>({ url: '/api/v1/blocklist/{id}', ...options });

export const bootstrap = <ThrowOnError extends boolean = false>(options?: Options<BootstrapData, ThrowOnError>) => (options?.client ?? client).get<BootstrapResponses, unknown, ThrowOnError>({ url: '/api/v1/bootstrap', ...options });

/**
 * List upcoming episodes (from monitored shows) + movie releases in a date
 * range. No grouping or pagination — this is a small list for a calendar UI.
 */
export const calendar = <ThrowOnError extends boolean = false>(options?: Options<CalendarData, ThrowOnError>) => (options?.client ?? client).get<CalendarResponses, unknown, ThrowOnError>({ url: '/api/v1/calendar', ...options });

/**
 * `GET /api/v1/calendar.ics` — iCalendar feed for subscribing from
 * Google Calendar, Apple Calendar, Thunderbird, etc. Emits one
 * all-day `VEVENT` per upcoming episode + movie release in a fixed
 * 90-day forward window (plus 30 days back so recent airs don't
 * drop off immediately in the calendar app).
 *
 * Auth: supports `?api_key=` in the URL because calendar subscribers
 * can't set headers. The usual auth middleware already handles
 * that query fallback.
 */
export const calendarIcs = <ThrowOnError extends boolean = false>(options?: Options<CalendarIcsData, ThrowOnError>) => (options?.client ?? client).get<CalendarIcsResponses, unknown, ThrowOnError>({ url: '/api/v1/calendar.ics', ...options });

/**
 * `GET /api/v1/cast/devices` — every Chromecast we know about,
 * freshest mDNS sightings first then any manually-added rows.
 */
export const listDevices = <ThrowOnError extends boolean = false>(options?: Options<ListDevicesData, ThrowOnError>) => (options?.client ?? client).get<ListDevicesResponses, unknown, ThrowOnError>({ url: '/api/v1/cast/devices', ...options });

/**
 * `POST /api/v1/cast/devices` — manually add a Chromecast by IP.
 * Useful when mDNS is blocked (Docker bridge, corporate Wi-Fi,
 * AP-isolated guest networks).
 */
export const addDevice = <ThrowOnError extends boolean = false>(options: Options<AddDeviceData, ThrowOnError>) => (options.client ?? client).post<AddDeviceResponses, AddDeviceErrors, ThrowOnError>({
    url: '/api/v1/cast/devices',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/v1/cast/devices/{id}` — forget a manually-added
 * device. mDNS-sourced rows are managed by discovery and ignore
 * the delete.
 */
export const deleteDevice = <ThrowOnError extends boolean = false>(options: Options<DeleteDeviceData, ThrowOnError>) => (options.client ?? client).delete<DeleteDeviceResponses, DeleteDeviceErrors, ThrowOnError>({ url: '/api/v1/cast/devices/{id}', ...options });

/**
 * `POST /api/v1/cast/sessions` — launch the receiver app on a
 * device and load the requested media. Returns immediately with
 * the new session row; status updates flow over WebSocket as the
 * session progresses.
 */
export const startSession = <ThrowOnError extends boolean = false>(options: Options<StartSessionData, ThrowOnError>) => (options.client ?? client).post<StartSessionResponses, StartSessionErrors, ThrowOnError>({
    url: '/api/v1/cast/sessions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/v1/cast/sessions/{id}` — stop the receiver app and
 * tear the worker thread down. Idempotent.
 */
export const stopSession = <ThrowOnError extends boolean = false>(options: Options<StopSessionData, ThrowOnError>) => (options.client ?? client).delete<StopSessionResponses, StopSessionErrors, ThrowOnError>({ url: '/api/v1/cast/sessions/{id}', ...options });

/**
 * `GET /api/v1/cast/sessions/{id}` — current row + last status JSON.
 */
export const getSession = <ThrowOnError extends boolean = false>(options: Options<GetSessionData, ThrowOnError>) => (options.client ?? client).get<GetSessionResponses, GetSessionErrors, ThrowOnError>({ url: '/api/v1/cast/sessions/{id}', ...options });

export const pause = <ThrowOnError extends boolean = false>(options: Options<PauseData, ThrowOnError>) => (options.client ?? client).post<PauseResponses, PauseErrors, ThrowOnError>({ url: '/api/v1/cast/sessions/{id}/pause', ...options });

export const play = <ThrowOnError extends boolean = false>(options: Options<PlayData, ThrowOnError>) => (options.client ?? client).post<PlayResponses, PlayErrors, ThrowOnError>({ url: '/api/v1/cast/sessions/{id}/play', ...options });

export const seek = <ThrowOnError extends boolean = false>(options: Options<SeekData, ThrowOnError>) => (options.client ?? client).post<SeekResponses, SeekErrors, ThrowOnError>({
    url: '/api/v1/cast/sessions/{id}/seek',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ingest a batch of frontend-originated log entries.
 */
export const ingestClientLogs = <ThrowOnError extends boolean = false>(options: Options<IngestClientLogsData, ThrowOnError>) => (options.client ?? client).post<IngestClientLogsResponses, IngestClientLogsErrors, ThrowOnError>({
    url: '/api/v1/client-logs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get application configuration (sensitive fields masked).
 */
export const getConfig = <ThrowOnError extends boolean = false>(options?: Options<GetConfigData, ThrowOnError>) => (options?.client ?? client).get<GetConfigResponses, GetConfigErrors, ThrowOnError>({ url: '/api/v1/config', ...options });

export const updateConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateConfigData, ThrowOnError>) => (options.client ?? client).put<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError>({
    url: '/api/v1/config',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rotate the API key. Generates a fresh UUID, writes it, and returns
 * the new config so the caller (the frontend, after confirming) can
 * update its stored key.
 */
export const rotateApiKey = <ThrowOnError extends boolean = false>(options?: Options<RotateApiKeyData, ThrowOnError>) => (options?.client ?? client).post<RotateApiKeyResponses, RotateApiKeyErrors, ThrowOnError>({ url: '/api/v1/config/rotate-api-key', ...options });

/**
 * `GET /api/v1/diagnostics/export` — build + return a ZIP bundle.
 *
 * Probe-style endpoint: doesn't mutate state, doesn't emit
 * `AppEvent`. The frontend offers this as a "Download diagnostic
 * bundle" button under Settings → Diagnostics.
 */
export const exportBundle = <ThrowOnError extends boolean = false>(options?: Options<ExportBundleData, ThrowOnError>) => (options?.client ?? client).get<ExportBundleResponses, unknown, ThrowOnError>({ url: '/api/v1/diagnostics/export', ...options });

/**
 * List downloads (queue view).
 *
 * Paginated per the `docs/subsystems/09-api.md` contract —
 * previous implementation capped at a silent `LIMIT 100` which
 * truncated bigger queues without telling the caller. Rows are
 * sorted by a phase-priority CASE expression (active first,
 * then pending, then terminal), breaking ties on `added_at DESC`.
 * The cursor carries both the phase-bucket ordinal and the `id`
 * so pagination is stable across state flips happening between
 * pages.
 */
export const listDownloads = <ThrowOnError extends boolean = false>(options?: Options<ListDownloadsData, ThrowOnError>) => (options?.client ?? client).get<ListDownloadsResponses, unknown, ThrowOnError>({ url: '/api/v1/downloads', ...options });

/**
 * `POST /api/v1/downloads/speed-test` — measures egress throughput by
 * downloading a known-size payload. Useful for setting realistic
 * Download Limit values and diagnosing slow downloads. Not routed
 * through the `BitTorrent` client, so it reflects the server's raw
 * network capability.
 *
 * Source priority: Cloudflare's speedtest (global anycast) first,
 * with a Hetzner fallback when Cloudflare bot-blocks us. We set a
 * browser User-Agent because the default reqwest UA gets a 403.
 */
export const speedTest = <ThrowOnError extends boolean = false>(options?: Options<SpeedTestData, ThrowOnError>) => (options?.client ?? client).post<SpeedTestResponses, unknown, ThrowOnError>({ url: '/api/v1/downloads/speed-test', ...options });

/**
 * Cancel and remove a download.
 */
export const cancelDownload = <ThrowOnError extends boolean = false>(options: Options<CancelDownloadData, ThrowOnError>) => (options.client ?? client).delete<CancelDownloadResponses, CancelDownloadErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}', ...options });

/**
 * Get a download by ID.
 */
export const getDownload = <ThrowOnError extends boolean = false>(options: Options<GetDownloadData, ThrowOnError>) => (options.client ?? client).get<GetDownloadResponses, GetDownloadErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}', ...options });

/**
 * Blocklist this download's release and search for another.
 */
export const blocklistAndSearch = <ThrowOnError extends boolean = false>(options: Options<BlocklistAndSearchData, ThrowOnError>) => (options.client ?? client).post<BlocklistAndSearchResponses, BlocklistAndSearchErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/blocklist-and-search', ...options });

/**
 * `GET /api/v1/downloads/{id}/files` — enumerate the torrent's files
 * with their current selection state. Returns `metadata_pending` = true
 * when librqbit is still resolving the info-dict (magnet just added).
 */
export const downloadFiles = <ThrowOnError extends boolean = false>(options: Options<DownloadFilesData, ThrowOnError>) => (options.client ?? client).get<DownloadFilesResponses, DownloadFilesErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/files', ...options });

/**
 * `POST /api/v1/downloads/{id}/files/select` — update which files in
 * a multi-file torrent are actively downloaded. Empty list means
 * "nothing" (effectively pauses the torrent); passing every index
 * means "all files" (default on grab).
 */
export const updateDownloadFiles = <ThrowOnError extends boolean = false>(options: Options<UpdateDownloadFilesData, ThrowOnError>) => (options.client ?? client).post<UpdateDownloadFilesResponses, UpdateDownloadFilesErrors, ThrowOnError>({
    url: '/api/v1/downloads/{id}/files/select',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Pause a download.
 */
export const pauseDownload = <ThrowOnError extends boolean = false>(options: Options<PauseDownloadData, ThrowOnError>) => (options.client ?? client).post<PauseDownloadResponses, PauseDownloadErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/pause', ...options });

/**
 * `GET /api/v1/downloads/{id}/peers` — snapshot of peer connections
 * for this torrent. Returns `not_live = true` instead of an error
 * when the torrent is paused so the UI can render "(paused)" cleanly.
 */
export const downloadPeers = <ThrowOnError extends boolean = false>(options: Options<DownloadPeersData, ThrowOnError>) => (options.client ?? client).get<DownloadPeersResponses, DownloadPeersErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/peers', ...options });

/**
 * `GET /api/v1/downloads/{id}/pieces` — per-piece have bitmap for the
 * Pieces tab's canvas rendering. Transport is base64 over JSON rather
 * than a raw binary endpoint because our OpenAPI-driven SDK codegen
 * only understands JSON responses; the bitmap is small (thousands of
 * bytes at most — ten megabits per thousand pieces).
 */
export const downloadPieces = <ThrowOnError extends boolean = false>(options: Options<DownloadPiecesData, ThrowOnError>) => (options.client ?? client).get<DownloadPiecesResponses, DownloadPiecesErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/pieces', ...options });

/**
 * Resume a paused download.
 */
export const resumeDownload = <ThrowOnError extends boolean = false>(options: Options<ResumeDownloadData, ThrowOnError>) => (options.client ?? client).post<ResumeDownloadResponses, ResumeDownloadErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/resume', ...options });

/**
 * Retry a failed download.
 */
export const retryDownload = <ThrowOnError extends boolean = false>(options: Options<RetryDownloadData, ThrowOnError>) => (options.client ?? client).post<RetryDownloadResponses, RetryDownloadErrors, ThrowOnError>({ url: '/api/v1/downloads/{id}/retry', ...options });

/**
 * `POST /api/v1/episodes/acquire-by-tmdb` — acquire a single
 * episode identified by `(show_tmdb_id, season, episode)`. When the
 * show isn't in the library yet, auto-follows it with "future only"
 * monitoring (same light-commitment pattern `watch_now_episode_by_tmdb`
 * uses) so the user's intent to grab this one episode doesn't
 * accidentally bulk-grab the entire back-catalog.
 *
 * Symmetric with `/episodes/{id}/acquire` for the in-library case
 * but also handles the cold-start: user on a TMDB show detail
 * clicks "Get" on S03E07 without ever following the show.
 */
export const acquireEpisodeByTmdb = <ThrowOnError extends boolean = false>(options: Options<AcquireEpisodeByTmdbData, ThrowOnError>) => (options.client ?? client).post<AcquireEpisodeByTmdbResponses, AcquireEpisodeByTmdbErrors, ThrowOnError>({
    url: '/api/v1/episodes/acquire-by-tmdb',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/v1/episodes/{id}/acquire` — flip an episode's
 * `acquire` bit on so the scheduler's wanted-sweep picks it up.
 * Used by the "Get" button on episode cards: when a show was
 * followed with "future only" (or seasons were unmonitored via the
 * Manage dialog), the user can still explicitly request a single
 * episode without changing season-wide settings.
 *
 * Minimal side effects: no media / download cleanup (nothing to
 * clean for an un-imported episode), just flip the bit and clear
 * the search debounce so the next sweep tick considers it
 * immediately. Scheduler sweep cadence is a few seconds — users
 * perceive the card flip to "Searching" as near-instant.
 */
export const acquireEpisode = <ThrowOnError extends boolean = false>(options: Options<AcquireEpisodeData, ThrowOnError>) => (options.client ?? client).post<AcquireEpisodeResponses, AcquireEpisodeErrors, ThrowOnError>({ url: '/api/v1/episodes/{id}/acquire', ...options });

/**
 * `POST /api/v1/episodes/{id}/discard` — inverse of `acquire`.
 * Symmetric with the episode card's "X" button: the user no longer
 * wants this episode. Walks back the acquisition completely:
 * 1. Cancel any in-flight download for the episode (stops librqbit
 * + emits `DownloadCancelled` so the UI snaps across tabs).
 * 2. Delete linked `media_episode` rows + prune orphan `media`
 * (file left on disk; user reclaims manually if desired).
 * 3. Set `acquire = 0` so the scheduler stops trying.
 */
export const discardEpisode = <ThrowOnError extends boolean = false>(options: Options<DiscardEpisodeData, ThrowOnError>) => (options.client ?? client).post<DiscardEpisodeResponses, DiscardEpisodeErrors, ThrowOnError>({ url: '/api/v1/episodes/{id}/discard', ...options });

/**
 * `POST /api/v1/episodes/{id}/redownload` — reset an imported
 * episode so the scheduler (or next Play click) re-searches for a
 * new release. Unlinks the current media but doesn't delete the
 * file on disk — user can remove it manually if desired; cleanup
 * is conservative because accidental redownload shouldn't torch the
 * existing copy while the new one is being fetched.
 */
export const redownloadEpisode = <ThrowOnError extends boolean = false>(options: Options<RedownloadEpisodeData, ThrowOnError>) => (options.client ?? client).post<RedownloadEpisodeResponses, RedownloadEpisodeErrors, ThrowOnError>({ url: '/api/v1/episodes/{id}/redownload', ...options });

/**
 * Per-episode release history — every release we've seen from any
 * indexer for this episode, with download + blocklist state joined
 * so the UI can render a one-glance "what did we try, what's pending"
 * view. Powers the manual-picker drawer and "why isn't this episode
 * downloading?" diagnostics.
 */
export const episodeReleases = <ThrowOnError extends boolean = false>(options: Options<EpisodeReleasesData, ThrowOnError>) => (options.client ?? client).get<EpisodeReleasesResponses, EpisodeReleasesErrors, ThrowOnError>({ url: '/api/v1/episodes/{id}/releases', ...options });

/**
 * `DELETE /api/v1/episodes/{id}/watched` — undo a "mark as watched"
 * so the episode reappears in Next Up.
 */
export const unmarkEpisodeWatched = <ThrowOnError extends boolean = false>(options: Options<UnmarkEpisodeWatchedData, ThrowOnError>) => (options.client ?? client).delete<UnmarkEpisodeWatchedResponses, UnmarkEpisodeWatchedErrors, ThrowOnError>({ url: '/api/v1/episodes/{id}/watched', ...options });

/**
 * `POST /api/v1/episodes/{id}/watched` — mark a single episode as
 * watched. Used by the episode-row overflow menu when the user has
 * already seen it elsewhere and wants Next Up to move on.
 */
export const markEpisodeWatched = <ThrowOnError extends boolean = false>(options: Options<MarkEpisodeWatchedData, ThrowOnError>) => (options.client ?? client).post<MarkEpisodeWatchedResponses, MarkEpisodeWatchedErrors, ThrowOnError>({ url: '/api/v1/episodes/{id}/watched', ...options });

/**
 * `GET /api/v1/fs/browse?path=…` — list directory contents. Hidden
 * entries (names starting with `.`) are excluded. When the
 * requested path doesn't exist, walks up to the nearest existing
 * ancestor and lists that — `result.fallback_from` carries the
 * original input so the UI can show "couldn't open X, showing Y".
 */
export const browse = <ThrowOnError extends boolean = false>(options?: Options<BrowseData, ThrowOnError>) => (options?.client ?? client).get<BrowseResponses, BrowseErrors, ThrowOnError>({ url: '/api/v1/fs/browse', ...options });

/**
 * `POST /api/v1/fs/mkdir` — create a directory (recursive). Used
 * by the path-picker's "+ New folder" affordance so users on a
 * fresh install don't have to drop into a terminal to set up
 * `/var/lib/kino/library` etc.
 */
export const mkdir = <ThrowOnError extends boolean = false>(options: Options<MkdirData, ThrowOnError>) => (options.client ?? client).post<MkdirResponses, MkdirErrors, ThrowOnError>({
    url: '/api/v1/fs/mkdir',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/v1/fs/mounts` — enumerate user-relevant mount points
 * (local disks, USB drives, NFS / SMB shares). Excludes kernel /
 * container plumbing (procfs, sysfs, tmpfs, cgroup, overlay) so
 * the path-picker sidebar only surfaces things the user might
 * actually want to navigate to.
 *
 * Linux: parses `/proc/mounts` directly (stable interface, no
 * extra deps). macOS / Windows: returns an empty list for now —
 * the "Common locations" fallback in the picker UI covers
 * `/Volumes` etc. until per-OS mount enumeration lands.
 */
export const mounts = <ThrowOnError extends boolean = false>(options?: Options<MountsData, ThrowOnError>) => (options?.client ?? client).get<MountsResponses, unknown, ThrowOnError>({ url: '/api/v1/fs/mounts', ...options });

/**
 * `GET /api/v1/fs/places` — produce the path-picker sidebar entries
 * the way GNOME Files / Finder / Explorer do: only paths that
 * exist, that the service can read, and (for top-level system
 * dirs) only if they actually contain something. Prevents dead
 * links and silent "click does nothing" UX from /Volumes on
 * Linux, an empty /mnt, or other-user homes the service can't
 * see.
 */
export const places = <ThrowOnError extends boolean = false>(options?: Options<PlacesData, ThrowOnError>) => (options?.client ?? client).get<PlacesResponses, unknown, ThrowOnError>({ url: '/api/v1/fs/places', ...options });

/**
 * `POST /api/v1/fs/test` — stat a path and report whether it's a
 * usable target for library / download storage.
 */
export const testPath = <ThrowOnError extends boolean = false>(options: Options<TestPathData, ThrowOnError>) => (options.client ?? client).get<TestPathResponses, unknown, ThrowOnError>({ url: '/api/v1/fs/test', ...options });

/**
 * `GET /api/v1/health` — one-shot snapshot of every dashboard panel.
 * Panels collect in parallel; a stuck collector times out at 500ms
 * and is marked `unknown` so the rest of the page still loads.
 */
export const getHealth = <ThrowOnError extends boolean = false>(options?: Options<GetHealthData, ThrowOnError>) => (options?.client ?? client).get<GetHealthResponses, unknown, ThrowOnError>({ url: '/api/v1/health', ...options });

/**
 * List history events (paginated, newest first).
 */
export const listHistory = <ThrowOnError extends boolean = false>(options?: Options<ListHistoryData, ThrowOnError>) => (options?.client ?? client).get<ListHistoryResponses, unknown, ThrowOnError>({ url: '/api/v1/history', ...options });

/**
 * `GET /api/v1/home/up-next` — the composed Up Next list.
 * Mixes paused movies, paused episodes, next-episode picks, and (as
 * padding, when the above signals yield <`UP_NEXT_MIN`) unwatched
 * recently-added movies. Sorted most-recently-relevant-first, capped
 * at `UP_NEXT_CAP`.
 *
 * Matches spec §18 § Up Next. The backend composes the row server-
 * side (single endpoint, ordered array) so the frontend doesn't need
 * to merge multiple queries.
 */
export const upNext = <ThrowOnError extends boolean = false>(options?: Options<UpNextData, ThrowOnError>) => (options?.client ?? client).get<UpNextResponses, unknown, ThrowOnError>({ url: '/api/v1/home/up-next', ...options });

/**
 * Serve a cached image with on-demand resizing.
 *
 * `content_type`: "movies" or "shows"
 * `image_type`: "poster" or "backdrop"
 */
export const getImage = <ThrowOnError extends boolean = false>(options: Options<GetImageData, ThrowOnError>) => (options.client ?? client).get<GetImageResponses, GetImageErrors, ThrowOnError>({ url: '/api/v1/images/{content_type}/{id}/{image_type}', ...options });

/**
 * List available indexer definitions.
 */
export const listDefinitions = <ThrowOnError extends boolean = false>(options?: Options<ListDefinitionsData, ThrowOnError>) => (options?.client ?? client).get<ListDefinitionsResponses, unknown, ThrowOnError>({ url: '/api/v1/indexer-definitions', ...options });

/**
 * Snapshot of the current refresh state. Authoritative for
 * late-joining clients (page reload mid-refresh).
 */
export const getRefreshState = <ThrowOnError extends boolean = false>(options?: Options<GetRefreshStateData, ThrowOnError>) => (options?.client ?? client).get<GetRefreshStateResponses, unknown, ThrowOnError>({ url: '/api/v1/indexer-definitions/refresh', ...options });

/**
 * Kick off a Cardigann definitions refresh from the Prowlarr/Indexers
 * GitHub repo. Returns immediately with `202 Accepted`; progress
 * flows via `IndexerDefinitionsRefresh*` WS events + the GET state
 * snapshot below. A second call while one is running returns `409
 * Conflict`. The setup wizard's Library Sources step + Settings →
 * Indexers both use this endpoint.
 */
export const refreshDefinitions = <ThrowOnError extends boolean = false>(options?: Options<RefreshDefinitionsData, ThrowOnError>) => (options?.client ?? client).post<RefreshDefinitionsResponses, RefreshDefinitionsErrors, ThrowOnError>({ url: '/api/v1/indexer-definitions/refresh', ...options });

/**
 * Get full details for an indexer definition.
 */
export const getDefinition = <ThrowOnError extends boolean = false>(options: Options<GetDefinitionData, ThrowOnError>) => (options.client ?? client).get<GetDefinitionResponses, GetDefinitionErrors, ThrowOnError>({ url: '/api/v1/indexer-definitions/{id}', ...options });

/**
 * List all indexers.
 */
export const listIndexers = <ThrowOnError extends boolean = false>(options?: Options<ListIndexersData, ThrowOnError>) => (options?.client ?? client).get<ListIndexersResponses, unknown, ThrowOnError>({ url: '/api/v1/indexers', ...options });

/**
 * Create an indexer.
 */
export const createIndexer = <ThrowOnError extends boolean = false>(options: Options<CreateIndexerData, ThrowOnError>) => (options.client ?? client).post<CreateIndexerResponses, unknown, ThrowOnError>({
    url: '/api/v1/indexers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an indexer.
 */
export const deleteIndexer = <ThrowOnError extends boolean = false>(options: Options<DeleteIndexerData, ThrowOnError>) => (options.client ?? client).delete<DeleteIndexerResponses, DeleteIndexerErrors, ThrowOnError>({ url: '/api/v1/indexers/{id}', ...options });

/**
 * Get an indexer by ID.
 */
export const getIndexer = <ThrowOnError extends boolean = false>(options: Options<GetIndexerData, ThrowOnError>) => (options.client ?? client).get<GetIndexerResponses, GetIndexerErrors, ThrowOnError>({ url: '/api/v1/indexers/{id}', ...options });

/**
 * Update an indexer.
 */
export const updateIndexer = <ThrowOnError extends boolean = false>(options: Options<UpdateIndexerData, ThrowOnError>) => (options.client ?? client).put<UpdateIndexerResponses, UpdateIndexerErrors, ThrowOnError>({
    url: '/api/v1/indexers/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reset an indexer's escalation backoff — clears `escalation_level`,
 * `initial_failure_time`, `most_recent_failure_time` and `disabled_until`
 * so the next scheduled health sweep (or search) treats it as healthy.
 * Useful when an indexer has been disabled due to transient errors and
 * the user has fixed the root cause.
 */
export const retryIndexer = <ThrowOnError extends boolean = false>(options: Options<RetryIndexerData, ThrowOnError>) => (options.client ?? client).post<RetryIndexerResponses, RetryIndexerErrors, ThrowOnError>({ url: '/api/v1/indexers/{id}/retry', ...options });

/**
 * Test an indexer by running a search for "test".
 */
export const testIndexer = <ThrowOnError extends boolean = false>(options: Options<TestIndexerData, ThrowOnError>) => (options.client ?? client).post<TestIndexerResponses, TestIndexerErrors, ThrowOnError>({ url: '/api/v1/indexers/{id}/test', ...options });

export const beginDevice = <ThrowOnError extends boolean = false>(options?: Options<BeginDeviceData, ThrowOnError>) => (options?.client ?? client).post<BeginDeviceResponses, BeginDeviceErrors, ThrowOnError>({ url: '/api/v1/integrations/trakt/device-code', ...options });

export const pollDevice = <ThrowOnError extends boolean = false>(options: Options<PollDeviceData, ThrowOnError>) => (options.client ?? client).post<PollDeviceResponses, unknown, ThrowOnError>({
    url: '/api/v1/integrations/trakt/device-poll',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const disconnect = <ThrowOnError extends boolean = false>(options?: Options<DisconnectData, ThrowOnError>) => (options?.client ?? client).post<DisconnectResponses, DisconnectErrors, ThrowOnError>({ url: '/api/v1/integrations/trakt/disconnect', ...options });

export const dryRun = <ThrowOnError extends boolean = false>(options?: Options<DryRunData, ThrowOnError>) => (options?.client ?? client).get<DryRunResponses, DryRunErrors, ThrowOnError>({ url: '/api/v1/integrations/trakt/dry-run', ...options });

export const import_ = <ThrowOnError extends boolean = false>(options?: Options<ImportData, ThrowOnError>) => (options?.client ?? client).post<ImportResponses, ImportErrors, ThrowOnError>({ url: '/api/v1/integrations/trakt/import', ...options });

export const rate = <ThrowOnError extends boolean = false>(options: Options<RateData, ThrowOnError>) => (options.client ?? client).post<RateResponses, RateErrors, ThrowOnError>({
    url: '/api/v1/integrations/trakt/rate/{kind}/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const recommendations = <ThrowOnError extends boolean = false>(options?: Options<RecommendationsData, ThrowOnError>) => (options?.client ?? client).get<RecommendationsResponses, unknown, ThrowOnError>({ url: '/api/v1/integrations/trakt/recommendations', ...options });

export const status = <ThrowOnError extends boolean = false>(options?: Options<StatusData, ThrowOnError>) => (options?.client ?? client).get<StatusResponses, unknown, ThrowOnError>({ url: '/api/v1/integrations/trakt/status', ...options });

export const syncNow = <ThrowOnError extends boolean = false>(options?: Options<SyncNowData, ThrowOnError>) => (options?.client ?? client).post<SyncNowResponses, SyncNowErrors, ThrowOnError>({ url: '/api/v1/integrations/trakt/sync', ...options });

export const trending = <ThrowOnError extends boolean = false>(options?: Options<TrendingData, ThrowOnError>) => (options?.client ?? client).get<TrendingResponses, unknown, ThrowOnError>({ url: '/api/v1/integrations/trakt/trending', ...options });

/**
 * Search the user's library (movies + shows) by title substring.
 */
export const librarySearch = <ThrowOnError extends boolean = false>(options: Options<LibrarySearchData, ThrowOnError>) => (options.client ?? client).get<LibrarySearchResponses, unknown, ThrowOnError>({ url: '/api/v1/library/search', ...options });

export const listLists = <ThrowOnError extends boolean = false>(options?: Options<ListListsData, ThrowOnError>) => (options?.client ?? client).get<ListListsResponses, unknown, ThrowOnError>({ url: '/api/v1/lists', ...options });

export const createList = <ThrowOnError extends boolean = false>(options: Options<CreateListData, ThrowOnError>) => (options.client ?? client).post<CreateListResponses, CreateListErrors, ThrowOnError>({
    url: '/api/v1/lists',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const deleteList = <ThrowOnError extends boolean = false>(options: Options<DeleteListData, ThrowOnError>) => (options.client ?? client).delete<DeleteListResponses, DeleteListErrors, ThrowOnError>({ url: '/api/v1/lists/{id}', ...options });

export const getList = <ThrowOnError extends boolean = false>(options: Options<GetListData, ThrowOnError>) => (options.client ?? client).get<GetListResponses, GetListErrors, ThrowOnError>({ url: '/api/v1/lists/{id}', ...options });

export const listItems = <ThrowOnError extends boolean = false>(options: Options<ListItemsData, ThrowOnError>) => (options.client ?? client).get<ListItemsResponses, ListItemsErrors, ThrowOnError>({ url: '/api/v1/lists/{id}/items', ...options });

export const ignoreItem = <ThrowOnError extends boolean = false>(options: Options<IgnoreItemData, ThrowOnError>) => (options.client ?? client).post<IgnoreItemResponses, IgnoreItemErrors, ThrowOnError>({
    url: '/api/v1/lists/{id}/items/{item_id}/ignore',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const refreshList = <ThrowOnError extends boolean = false>(options: Options<RefreshListData, ThrowOnError>) => (options.client ?? client).post<RefreshListResponses, RefreshListErrors, ThrowOnError>({ url: '/api/v1/lists/{id}/refresh', ...options });

export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>) => (options?.client ?? client).post<LogoutResponses, unknown, ThrowOnError>({ url: '/api/v1/logout', ...options });

/**
 * Query recent log entries.
 */
export const listLogs = <ThrowOnError extends boolean = false>(options?: Options<ListLogsData, ThrowOnError>) => (options?.client ?? client).get<ListLogsResponses, unknown, ThrowOnError>({ url: '/api/v1/logs', ...options });

/**
 * Export matching log entries as NDJSON (one JSON object per line).
 *
 * Uses the same filter set as `list_logs`. Ordered chronologically
 * (ascending `ts_us`) which is the natural shape for a log file —
 * easier to grep, easier to tail, easier to re-ingest. Capped at
 * 100k rows to keep memory predictable for a home server.
 */
export const exportLogs = <ThrowOnError extends boolean = false>(options?: Options<ExportLogsData, ThrowOnError>) => (options?.client ?? client).get<ExportLogsResponses, unknown, ThrowOnError>({ url: '/api/v1/logs/export', ...options });

/**
 * List media files, optionally filtered by movie or episode.
 *
 * Paginated per `docs/subsystems/09-api.md` — the prior
 * implementation silently capped at `LIMIT 100`, which truncated
 * larger libraries without a `has_more` signal so clients
 * couldn't tell they were missing rows. Ordering is `date_added
 * DESC, id DESC` (newest first, stable tiebreak).
 *
 * The filtered branches (by `movie_id` or `episode_id`) also
 * paginate — historically those returned every row, but any
 * pathological case of 300+ upgraded copies for the same
 * title would be just as dangerous there.
 */
export const listMedia = <ThrowOnError extends boolean = false>(options?: Options<ListMediaData, ThrowOnError>) => (options?.client ?? client).get<ListMediaResponses, unknown, ThrowOnError>({ url: '/api/v1/media', ...options });

/**
 * Delete a media file and its streams.
 */
export const deleteMedia = <ThrowOnError extends boolean = false>(options: Options<DeleteMediaData, ThrowOnError>) => (options.client ?? client).delete<DeleteMediaResponses, DeleteMediaErrors, ThrowOnError>({ url: '/api/v1/media/{id}', ...options });

/**
 * Get a media file by ID.
 */
export const getMedia = <ThrowOnError extends boolean = false>(options: Options<GetMediaData, ThrowOnError>) => (options.client ?? client).get<GetMediaResponses, GetMediaErrors, ThrowOnError>({ url: '/api/v1/media/{id}', ...options });

/**
 * Get streams for a media file.
 */
export const getMediaStreams = <ThrowOnError extends boolean = false>(options: Options<GetMediaStreamsData, ThrowOnError>) => (options.client ?? client).get<GetMediaStreamsResponses, GetMediaStreamsErrors, ThrowOnError>({ url: '/api/v1/media/{id}/streams', ...options });

/**
 * `POST /api/v1/metadata/test-opensubtitles` — attempts a login with
 * the stored `OpenSubtitles` credentials. Reports the specific HTTP
 * response verbatim on failure so the user sees whether it's the key,
 * the username, or the password that's wrong.
 */
export const testOpensubtitles = <ThrowOnError extends boolean = false>(options?: Options<TestOpensubtitlesData, ThrowOnError>) => (options?.client ?? client).post<TestOpensubtitlesResponses, unknown, ThrowOnError>({ url: '/api/v1/metadata/test-opensubtitles', ...options });

/**
 * `POST /api/v1/metadata/test-tmdb` — hit a stable known endpoint
 * (The Matrix, id 603) and confirm the key is accepted. More reliable
 * than `/search?q=test` which can succeed for unrelated reasons.
 */
export const testTmdb = <ThrowOnError extends boolean = false>(options?: Options<TestTmdbData, ThrowOnError>) => (options?.client ?? client).post<TestTmdbResponses, unknown, ThrowOnError>({ url: '/api/v1/metadata/test-tmdb', ...options });

/**
 * List movies (paginated).
 */
export const listMovies = <ThrowOnError extends boolean = false>(options?: Options<ListMoviesData, ThrowOnError>) => (options?.client ?? client).get<ListMoviesResponses, unknown, ThrowOnError>({ url: '/api/v1/movies', ...options });

/**
 * Request a movie (create from TMDB and set as wanted).
 */
export const createMovie = <ThrowOnError extends boolean = false>(options: Options<CreateMovieData, ThrowOnError>) => (options.client ?? client).post<CreateMovieResponses, CreateMovieErrors, ThrowOnError>({
    url: '/api/v1/movies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a movie.
 */
export const deleteMovie = <ThrowOnError extends boolean = false>(options: Options<DeleteMovieData, ThrowOnError>) => (options.client ?? client).delete<DeleteMovieResponses, DeleteMovieErrors, ThrowOnError>({ url: '/api/v1/movies/{id}', ...options });

/**
 * Get a movie by ID.
 */
export const getMovie = <ThrowOnError extends boolean = false>(options: Options<GetMovieData, ThrowOnError>) => (options.client ?? client).get<GetMovieResponses, GetMovieErrors, ThrowOnError>({ url: '/api/v1/movies/{id}', ...options });

/**
 * Per-movie release history — same shape as `episode_releases` but
 * scoped to a `movie_id`.
 */
export const movieReleases = <ThrowOnError extends boolean = false>(options: Options<MovieReleasesData, ThrowOnError>) => (options.client ?? client).get<MovieReleasesResponses, MovieReleasesErrors, ThrowOnError>({ url: '/api/v1/movies/{id}/releases', ...options });

/**
 * Unmark content as watched (reset to available).
 */
export const unmarkMovieWatched = <ThrowOnError extends boolean = false>(options: Options<UnmarkMovieWatchedData, ThrowOnError>) => (options.client ?? client).delete<UnmarkMovieWatchedResponses, UnmarkMovieWatchedErrors, ThrowOnError>({ url: '/api/v1/movies/{id}/watched', ...options });

/**
 * Mark content as watched manually.
 */
export const markMovieWatched = <ThrowOnError extends boolean = false>(options: Options<MarkMovieWatchedData, ThrowOnError>) => (options.client ?? client).post<MarkMovieWatchedResponses, MarkMovieWatchedErrors, ThrowOnError>({ url: '/api/v1/movies/{id}/watched', ...options });

/**
 * `GET /api/v1/network/lan-probe` — non-loopback `IPv4s` + bound port.
 *
 * Public so the wizard can call it without holding a session yet.
 */
export const lanProbe = <ThrowOnError extends boolean = false>(options?: Options<LanProbeData, ThrowOnError>) => (options?.client ?? client).get<LanProbeResponses, unknown, ThrowOnError>({ url: '/api/v1/network/lan-probe', ...options });

/**
 * `POST /api/v1/network/mdns-test` — resolve `<hostname>.local`
 * from the backend's perspective. Validates that the responder is
 * live and that the local resolver chain can find it. Doesn't
 * guarantee LAN clients can — that's what `lan_probe` + browser
 * probe is for.
 */
export const mdnsTest = <ThrowOnError extends boolean = false>(options: Options<MdnsTestData, ThrowOnError>) => (options.client ?? client).post<MdnsTestResponses, unknown, ThrowOnError>({
    url: '/api/v1/network/mdns-test',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/v1/play/{kind}/{entity_id}/direct`
 *
 * Range-aware byte stream of the resolved source. Library source
 * goes through `tower_http::ServeFile` (local disk). Stream source
 * goes through librqbit's piece-prioritised `FileStream`. The
 * dispatcher decides fresh on every request — a browser issuing
 * sequential Range requests during playback transparently flips
 * from stream → library the moment the import lands.
 */
export const direct = <ThrowOnError extends boolean = false>(options: Options<DirectData, ThrowOnError>) => (options.client ?? client).get<DirectResponses, DirectErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/direct', ...options });

/**
 * `GET /api/v1/play/{kind}/{entity_id}/master.m3u8`
 *
 * Starts or reuses an HLS transcode session keyed on the entity.
 * Library source → ffmpeg reads the file path directly. Stream
 * source → ffmpeg reads our own `/direct` URL so librqbit's piece
 * prioritiser stays in the loop. Optional `?at=<secs>` sets
 * ffmpeg `-ss` for seek restarts; `?audio_stream=N` picks an
 * alternate audio track; `?tab=<nonce>` segregates sessions so
 * two browser tabs playing the same entity don't kill each
 * other's ffmpeg.
 */
export const hlsMaster = <ThrowOnError extends boolean = false>(options: Options<HlsMasterData, ThrowOnError>) => (options.client ?? client).get<HlsMasterResponses, HlsMasterErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/master.m3u8', ...options });

/**
 * `GET /api/v1/play/{kind}/{entity_id}/prepare`
 *
 * Returns everything the unified player needs to render. Never
 * fails with a raw 500 if possible — errors become `state: failed`
 * + `error_message`.
 */
export const prepare = <ThrowOnError extends boolean = false>(options: Options<PrepareData, ThrowOnError>) => (options.client ?? client).get<PrepareResponses, PrepareErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/prepare', ...options });

/**
 * `POST /api/v1/play/{kind}/{entity_id}/progress`
 */
export const playProgress = <ThrowOnError extends boolean = false>(options: Options<PlayProgressData, ThrowOnError>) => (options.client ?? client).post<PlayProgressResponses, PlayProgressErrors, ThrowOnError>({
    url: '/api/v1/play/{kind}/{entity_id}/progress',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/v1/play/{kind}/{entity_id}/segments/{index}`
 */
export const hlsSegment = <ThrowOnError extends boolean = false>(options: Options<HlsSegmentData, ThrowOnError>) => (options.client ?? client).get<HlsSegmentResponses, HlsSegmentErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/segments/{index}', ...options });

/**
 * `GET /api/v1/play/{kind}/{entity_id}/subtitles/{stream_index}`
 *
 * Serves a `WebVTT` rendition of the requested subtitle stream.
 * Library sources read codec + path from the DB `stream` table;
 * streaming sources pull the same info from the in-memory probe
 * cache + partial torrent file. Both paths route through the
 * same `get_subtitle_path` extractor so SRT/ASS conversions
 * behave identically; cache roots differ
 * (`cache/subs/{media_id}` vs `cache/subs-stream/{download_id}`)
 * so a partial streaming extract doesn't collide with a library
 * rebuild if the user imports mid-stream.
 *
 * Accepts `?offset=<secs>` for HLS playback that started mid-file
 * via `ffmpeg -ss`. The VTT's native timestamps are source-time;
 * without the shift the browser would wait for the real-clock cue
 * time against the trimmed output's zero-based
 * `video.currentTime`, displaying subtitles minutes late (or not
 * at all if the trim is far enough in).
 */
export const subtitle = <ThrowOnError extends boolean = false>(options: Options<SubtitleData, ThrowOnError>) => (options.client ?? client).get<SubtitleResponses, SubtitleErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/subtitles/{stream_index}', ...options });

/**
 * `DELETE /api/v1/play/{kind}/{entity_id}/transcode`
 *
 * Called by the frontend on player unmount so the encoder budget
 * doesn't linger behind a closed tab.
 */
export const stopTranscode = <ThrowOnError extends boolean = false>(options: Options<StopTranscodeData, ThrowOnError>) => (options.client ?? client).delete<StopTranscodeResponses, unknown, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/transcode', ...options });

/**
 * `GET /api/v1/play/{kind}/{entity_id}/trickplay.vtt`
 *
 * Picks the right source's trickplay VTT: library-generated for
 * imported media (from `media.trickplay_generated`), stream-
 * generated for in-progress downloads (written by the streaming
 * trickplay task). During the stream→library transition the next
 * fetch naturally starts hitting the library VTT once it exists —
 * WS-driven invalidation on the frontend triggers the refetch.
 */
export const trickplayVtt = <ThrowOnError extends boolean = false>(options: Options<TrickplayVttData, ThrowOnError>) => (options.client ?? client).get<TrickplayVttResponses, TrickplayVttErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/trickplay.vtt', ...options });

/**
 * `GET /api/v1/play/{kind}/{entity_id}/trickplay/{name}` — JPEG
 * sprite sheet referenced by the VTT. Dispatches on byte source
 * so the same URL serves library-generated sprites pre- or
 * post-import, and stream-generated sprites during the live
 * download phase.
 */
export const trickplaySprite = <ThrowOnError extends boolean = false>(options: Options<TrickplaySpriteData, ThrowOnError>) => (options.client ?? client).get<TrickplaySpriteResponses, TrickplaySpriteErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/trickplay/{name}', ...options });

/**
 * `GET /api/v1/play/{kind}/{entity_id}/variant.m3u8`
 */
export const hlsVariant = <ThrowOnError extends boolean = false>(options: Options<HlsVariantData, ThrowOnError>) => (options.client ?? client).get<HlsVariantResponses, HlsVariantErrors, ThrowOnError>({ url: '/api/v1/play/{kind}/{entity_id}/variant.m3u8', ...options });

/**
 * `POST /api/v1/playback/cast-token` — issues a short-lived HMAC
 * token for a specific media file and returns the Cast-safe
 * stream URL to load on the receiver.
 *
 * The returned URL targets the live `/api/v1/play/{kind}/{entity_id}/…`
 * routes, resolving `media_id` to its `(kind, entity_id)` pair via
 * `media` (for movies) or `media_episode` (for TV). The token's
 * subject is the play-route pair (e.g. `movie/42`) so the
 * receiver can fetch `/master.m3u8`, variant playlists, segments,
 * subtitles, trickplay, and `/progress` under that prefix — and
 * only that prefix — without the API key. The auth middleware
 * (`auth::require_api_key`) verifies the `?cast_token=` query
 * param against the URL path on every request.
 */
export const issueCastToken = <ThrowOnError extends boolean = false>(options: Options<IssueCastTokenData, ThrowOnError>) => (options.client ?? client).post<IssueCastTokenResponses, IssueCastTokenErrors, ThrowOnError>({
    url: '/api/v1/playback/cast-token',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/v1/playback/ffmpeg/download` — cancel an
 * in-flight download. Idempotent: safe to call when no
 * download is running (no-op). The task will observe the
 * cancellation token at its next chunk boundary and move to
 * the `Failed { reason: "cancelled" }` state.
 */
export const cancelFfmpegDownload = <ThrowOnError extends boolean = false>(options?: Options<CancelFfmpegDownloadData, ThrowOnError>) => (options?.client ?? client).delete<CancelFfmpegDownloadResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/ffmpeg/download', ...options });

/**
 * `GET /api/v1/playback/ffmpeg/download` — returns the current
 * download state. Used by late-joining clients (e.g., a browser
 * refreshed mid-download) to reconstruct the progress bar
 * without waiting for the next broadcast tick.
 */
export const getFfmpegDownload = <ThrowOnError extends boolean = false>(options?: Options<GetFfmpegDownloadData, ThrowOnError>) => (options?.client ?? client).get<GetFfmpegDownloadResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/ffmpeg/download', ...options });

/**
 * `POST /api/v1/playback/ffmpeg/download` — starts a download
 * of the pinned jellyfin-ffmpeg build into `{data_path}/bin/`.
 * Returns 202 Accepted with the current state when a new
 * download starts; 409 Conflict when one is already running;
 * 400 when the host platform isn't in the pinned table.
 */
export const startFfmpegDownload = <ThrowOnError extends boolean = false>(options?: Options<StartFfmpegDownloadData, ThrowOnError>) => (options?.client ?? client).post<StartFfmpegDownloadResponses, StartFfmpegDownloadErrors, ThrowOnError>({ url: '/api/v1/playback/ffmpeg/download', ...options });

/**
 * `POST /api/v1/playback/ffmpeg/revert` — remove the bundled
 * ffmpeg and revert to the system ffmpeg. Clears
 * `config.ffmpeg_path` and deletes `{data_path}/bin/`.
 * Idempotent: safe when no bundle is installed.
 */
export const revertFfmpegToSystem = <ThrowOnError extends boolean = false>(options?: Options<RevertFfmpegToSystemData, ThrowOnError>) => (options?.client ?? client).post<RevertFfmpegToSystemResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/ffmpeg/revert', ...options });

/**
 * `POST /api/v1/playback/probe` — runs the configured ffmpeg
 * binary to confirm it exists, report its version, and run a
 * trial encode through every hardware backend. The response is
 * the same typed `HwCapabilities` shape the status banner +
 * (future) profile chain consume from the cache.
 */
export const probe = <ThrowOnError extends boolean = false>(options?: Options<ProbeData, ThrowOnError>) => (options?.client ?? client).post<ProbeResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/probe', ...options });

/**
 * `POST /api/v1/playback/test-transcode` — actually runs ffmpeg
 * to transcode a synthetic 2s test source using the
 * currently-configured hardware acceleration. Catches broken
 * drivers / missing codec libs that the lighter `probe`
 * endpoint doesn't exercise for the configured backend.
 */
export const testTranscode = <ThrowOnError extends boolean = false>(options?: Options<TestTranscodeData, ThrowOnError>) => (options?.client ?? client).post<TestTranscodeResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/test-transcode', ...options });

/**
 * `GET /api/v1/playback/transcode-sessions` — one row per live
 * session, with a title looked up from the media table so the UI
 * can show what's actually playing.
 */
export const transcodeSessions = <ThrowOnError extends boolean = false>(options?: Options<TranscodeSessionsData, ThrowOnError>) => (options?.client ?? client).get<TranscodeSessionsResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/transcode-sessions', ...options });

/**
 * `DELETE /api/v1/playback/transcode-sessions/{session_id}` —
 * stop one session by its raw id. Used by the settings page's
 * session list, which shows a mix of `play-*` and legacy
 * `transcode-*` session ids and needs an entity-agnostic kill.
 * The `/play/.../transcode` DELETE is entity-keyed and doesn't
 * fit this UI.
 */
export const stopTranscodeSession = <ThrowOnError extends boolean = false>(options: Options<StopTranscodeSessionData, ThrowOnError>) => (options.client ?? client).delete<StopTranscodeSessionResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/transcode-sessions/{session_id}', ...options });

/**
 * `GET /api/v1/playback/transcode-stats` — how many transcode
 * sessions are currently running. Cheap; used by the settings card.
 */
export const transcodeStats = <ThrowOnError extends boolean = false>(options?: Options<TranscodeStatsData, ThrowOnError>) => (options?.client ?? client).get<TranscodeStatsResponses, unknown, ThrowOnError>({ url: '/api/v1/playback/transcode-stats', ...options });

/**
 * `GET /api/v1/preferences/home` — current Home preferences.
 *
 * Never 404s: a fresh install with no row yet returns defaults and
 * inserts the row so the next write can `UPDATE` cleanly.
 */
export const getHomePreferences = <ThrowOnError extends boolean = false>(options?: Options<GetHomePreferencesData, ThrowOnError>) => (options?.client ?? client).get<GetHomePreferencesResponses, unknown, ThrowOnError>({ url: '/api/v1/preferences/home', ...options });

/**
 * `PATCH /api/v1/preferences/home` — partial update.
 *
 * Idempotent by construction: re-sending the same body is a no-op
 * beyond bumping `updated_at`. Arrays replace wholesale when present
 * (no per-element merging — the frontend always has the full list
 * from its most recent GET).
 */
export const updateHomePreferences = <ThrowOnError extends boolean = false>(options: Options<UpdateHomePreferencesData, ThrowOnError>) => (options.client ?? client).patch<UpdateHomePreferencesResponses, unknown, ThrowOnError>({
    url: '/api/v1/preferences/home',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/v1/preferences/home/reset` — wipe customisations back
 * to the v1 defaults. Used by the "Reset to defaults" button in the
 * Customise drawer.
 */
export const resetHomePreferences = <ThrowOnError extends boolean = false>(options?: Options<ResetHomePreferencesData, ThrowOnError>) => (options?.client ?? client).post<ResetHomePreferencesResponses, unknown, ThrowOnError>({ url: '/api/v1/preferences/home/reset', ...options });

/**
 * List all quality profiles with their usage counts so the settings
 * UI can show "in use by N items" badges and explain delete blocks.
 */
export const listQualityProfiles = <ThrowOnError extends boolean = false>(options?: Options<ListQualityProfilesData, ThrowOnError>) => (options?.client ?? client).get<ListQualityProfilesResponses, unknown, ThrowOnError>({ url: '/api/v1/quality-profiles', ...options });

/**
 * Create a new quality profile.
 */
export const createQualityProfile = <ThrowOnError extends boolean = false>(options: Options<CreateQualityProfileData, ThrowOnError>) => (options.client ?? client).post<CreateQualityProfileResponses, unknown, ThrowOnError>({
    url: '/api/v1/quality-profiles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a quality profile.
 */
export const deleteQualityProfile = <ThrowOnError extends boolean = false>(options: Options<DeleteQualityProfileData, ThrowOnError>) => (options.client ?? client).delete<DeleteQualityProfileResponses, DeleteQualityProfileErrors, ThrowOnError>({ url: '/api/v1/quality-profiles/{id}', ...options });

/**
 * Get a quality profile by ID.
 */
export const getQualityProfile = <ThrowOnError extends boolean = false>(options: Options<GetQualityProfileData, ThrowOnError>) => (options.client ?? client).get<GetQualityProfileResponses, GetQualityProfileErrors, ThrowOnError>({ url: '/api/v1/quality-profiles/{id}', ...options });

/**
 * Update a quality profile.
 */
export const updateQualityProfile = <ThrowOnError extends boolean = false>(options: Options<UpdateQualityProfileData, ThrowOnError>) => (options.client ?? client).put<UpdateQualityProfileResponses, UpdateQualityProfileErrors, ThrowOnError>({
    url: '/api/v1/quality-profiles/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Promote a quality profile to be the default. Clears the flag on
 * every other profile in the same transaction so the invariant
 * "exactly one default" is preserved.
 */
export const setDefaultQualityProfile = <ThrowOnError extends boolean = false>(options: Options<SetDefaultQualityProfileData, ThrowOnError>) => (options.client ?? client).post<SetDefaultQualityProfileResponses, SetDefaultQualityProfileErrors, ThrowOnError>({ url: '/api/v1/quality-profiles/{id}/set-default', ...options });

/**
 * List releases, optionally filtered by movie or episode.
 */
export const listReleases = <ThrowOnError extends boolean = false>(options?: Options<ListReleasesData, ThrowOnError>) => (options?.client ?? client).get<ListReleasesResponses, unknown, ThrowOnError>({ url: '/api/v1/releases', ...options });

/**
 * Grab a specific release — the manual-picker action behind the
 * "Releases" drawer. Routes through the same service helpers as
 * automatic grabs so the download gets a proper `download_content`
 * link (movie or episode), URL resolution via `resolve_release_url`,
 * release-status update, and the `ReleaseGrabbed` event.
 */
export const grabRelease = <ThrowOnError extends boolean = false>(options: Options<GrabReleaseData, ThrowOnError>) => (options.client ?? client).post<GrabReleaseResponses, GrabReleaseErrors, ThrowOnError>({ url: '/api/v1/releases/{id}/grab', ...options });

/**
 * Grab a release AND start the torrent synchronously so the client
 * can stream it immediately. Idempotent: if a download for this
 * release already exists, returns its id (or redirects to the
 * imported media if it's already landed in the library).
 */
export const grabAndWatch = <ThrowOnError extends boolean = false>(options: Options<GrabAndWatchData, ThrowOnError>) => (options.client ?? client).post<GrabAndWatchResponses, GrabAndWatchErrors, ThrowOnError>({ url: '/api/v1/releases/{id}/grab-and-watch', ...options });

export const revokeAll = <ThrowOnError extends boolean = false>(options?: Options<RevokeAllData, ThrowOnError>) => (options?.client ?? client).delete<RevokeAllResponses, RevokeAllErrors, ThrowOnError>({ url: '/api/v1/sessions', ...options });

export const listSessions = <ThrowOnError extends boolean = false>(options?: Options<ListSessionsData, ThrowOnError>) => (options?.client ?? client).get<ListSessionsResponses, unknown, ThrowOnError>({ url: '/api/v1/sessions', ...options });

/**
 * Exchange the master API key for a cookie session. This endpoint
 * is intentionally public — the body carries the credential — and
 * rate-limited at the layer above to slow brute-force.
 */
export const createSession = <ThrowOnError extends boolean = false>(options: Options<CreateSessionData, ThrowOnError>) => (options.client ?? client).post<CreateSessionResponses, CreateSessionErrors, ThrowOnError>({
    url: '/api/v1/sessions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const createBootstrapToken = <ThrowOnError extends boolean = false>(options?: Options<CreateBootstrapTokenData, ThrowOnError>) => (options?.client ?? client).post<CreateBootstrapTokenResponses, unknown, ThrowOnError>({ url: '/api/v1/sessions/bootstrap-token', ...options });

export const createCliToken = <ThrowOnError extends boolean = false>(options: Options<CreateCliTokenData, ThrowOnError>) => (options.client ?? client).post<CreateCliTokenResponses, unknown, ThrowOnError>({
    url: '/api/v1/sessions/cli',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redeem a one-time pairing token (issued via
 * `POST /sessions/bootstrap-token` and rendered as a QR code on the
 * originating device). On success: marks the bootstrap row consumed,
 * creates a fresh `qr-bootstrap` session, sets the cookie.
 */
export const redeem = <ThrowOnError extends boolean = false>(options: Options<RedeemData, ThrowOnError>) => (options.client ?? client).post<RedeemResponses, RedeemErrors, ThrowOnError>({
    url: '/api/v1/sessions/redeem',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const signUrl = <ThrowOnError extends boolean = false>(options: Options<SignUrlData, ThrowOnError>) => (options.client ?? client).post<SignUrlResponses, unknown, ThrowOnError>({
    url: '/api/v1/sessions/sign-url',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const revokeSession = <ThrowOnError extends boolean = false>(options: Options<RevokeSessionData, ThrowOnError>) => (options.client ?? client).delete<RevokeSessionResponses, RevokeSessionErrors, ThrowOnError>({ url: '/api/v1/sessions/{id}', ...options });

/**
 * List shows (paginated).
 */
export const listShows = <ThrowOnError extends boolean = false>(options?: Options<ListShowsData, ThrowOnError>) => (options?.client ?? client).get<ListShowsResponses, unknown, ThrowOnError>({ url: '/api/v1/shows', ...options });

/**
 * Follow a show (create from TMDB with all seasons/episodes).
 */
export const createShow = <ThrowOnError extends boolean = false>(options: Options<CreateShowData, ThrowOnError>) => (options.client ?? client).post<CreateShowResponses, CreateShowErrors, ThrowOnError>({
    url: '/api/v1/shows',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/v1/shows/by-tmdb/{tmdb_id}/seasons/{season}/episodes`
 *
 * Returns a list of `EpisodeView` — TMDB season details left-joined
 * with per-episode library state. One canonical endpoint replaces
 * the old branching between the TMDB proxy (for not-followed shows)
 * and the library episodes endpoint (for followed shows). The
 * `EpisodeCard` component can now always render the right thing
 * without asking "is the show in library?"
 */
export const showSeasonEpisodesByTmdb = <ThrowOnError extends boolean = false>(options: Options<ShowSeasonEpisodesByTmdbData, ThrowOnError>) => (options.client ?? client).get<ShowSeasonEpisodesByTmdbResponses, unknown, ThrowOnError>({ url: '/api/v1/shows/by-tmdb/{tmdb_id}/seasons/{season_number}/episodes', ...options });

/**
 * `GET /api/v1/shows/by-tmdb/{tmdb_id}/watch-state` — lightweight
 * progress summary so the `ShowDetail` page can render a prominent
 * "Play {S01E03}" CTA when the user is mid-series. 200 with
 * `followed: false` when the show isn't in the library — the UI
 * still shows a generic "Start watching" CTA in that case.
 */
export const showWatchState = <ThrowOnError extends boolean = false>(options: Options<ShowWatchStateData, ThrowOnError>) => (options.client ?? client).get<ShowWatchStateResponses, unknown, ThrowOnError>({ url: '/api/v1/shows/by-tmdb/{tmdb_id}/watch-state', ...options });

/**
 * Delete a show and all its seasons/episodes.
 */
export const deleteShow = <ThrowOnError extends boolean = false>(options: Options<DeleteShowData, ThrowOnError>) => (options.client ?? client).delete<DeleteShowResponses, DeleteShowErrors, ThrowOnError>({ url: '/api/v1/shows/{id}', ...options });

/**
 * Get a show by ID.
 */
export const getShow = <ThrowOnError extends boolean = false>(options: Options<GetShowData, ThrowOnError>) => (options.client ?? client).get<GetShowResponses, GetShowErrors, ThrowOnError>({ url: '/api/v1/shows/{id}', ...options });

/**
 * `PATCH /api/v1/shows/{id}/monitor` — re-apply monitor preferences
 * to an existing show (e.g. the Manage downloads flow). Does NOT
 * re-fetch TMDB or reinsert episode rows. Persists
 * `monitor_new_items` + `monitor_specials` on the show row, rewrites
 * per-episode `acquire`/`in_scope` to match the new season list, and
 * cancels in-flight downloads for episodes that just went out of
 * scope.
 */
export const updateShowMonitor = <ThrowOnError extends boolean = false>(options: Options<UpdateShowMonitorData, ThrowOnError>) => (options.client ?? client).patch<UpdateShowMonitorResponses, UpdateShowMonitorErrors, ThrowOnError>({
    url: '/api/v1/shows/{id}/monitor',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const monitoredSeasons = <ThrowOnError extends boolean = false>(options: Options<MonitoredSeasonsData, ThrowOnError>) => (options.client ?? client).get<MonitoredSeasonsResponses, MonitoredSeasonsErrors, ThrowOnError>({ url: '/api/v1/shows/{id}/monitored-seasons', ...options });

/**
 * `POST /api/v1/shows/{id}/pause-downloads` — pause every active
 * torrent linked to this show's episodes. User-intent surface: "stop
 * what this show is doing right now." Already-paused / terminal
 * rows are skipped. Per-download `DownloadPaused` events fan out so
 * other tabs flip instantly.
 *
 * This is a *snapshot* action, not a policy: new downloads started
 * after this call (e.g. by the scheduler finding a fresh episode)
 * run normally. Users who want "stop acquiring for this show" use
 * Manage Downloads to unmonitor seasons.
 */
export const pauseShowDownloads = <ThrowOnError extends boolean = false>(options: Options<PauseShowDownloadsData, ThrowOnError>) => (options.client ?? client).post<PauseShowDownloadsResponses, PauseShowDownloadsErrors, ThrowOnError>({ url: '/api/v1/shows/{id}/pause-downloads', ...options });

/**
 * `POST /api/v1/shows/{id}/resume-downloads` — inverse: unpause
 * every currently-paused torrent for this show. Doesn't touch
 * downloading / queued rows.
 */
export const resumeShowDownloads = <ThrowOnError extends boolean = false>(options: Options<ResumeShowDownloadsData, ThrowOnError>) => (options.client ?? client).post<ResumeShowDownloadsResponses, ResumeShowDownloadsErrors, ThrowOnError>({ url: '/api/v1/shows/{id}/resume-downloads', ...options });

/**
 * List seasons for a show.
 */
export const listSeasons = <ThrowOnError extends boolean = false>(options: Options<ListSeasonsData, ThrowOnError>) => (options.client ?? client).get<ListSeasonsResponses, unknown, ThrowOnError>({ url: '/api/v1/shows/{id}/seasons', ...options });

/**
 * Manually kick intro/credits analysis for a season. Fire-and-forget —
 * returns 202 Accepted immediately, work happens on a background task.
 * Used by the scheduled catch-up and any future admin tool.
 */
export const analyseSeasonIntro = <ThrowOnError extends boolean = false>(options: Options<AnalyseSeasonIntroData, ThrowOnError>) => (options.client ?? client).post<AnalyseSeasonIntroResponses, AnalyseSeasonIntroErrors, ThrowOnError>({ url: '/api/v1/shows/{id}/seasons/{season_number}/analyse-intro', ...options });

/**
 * List episodes for a season.
 */
export const listEpisodes = <ThrowOnError extends boolean = false>(options: Options<ListEpisodesData, ThrowOnError>) => (options.client ?? client).get<ListEpisodesResponses, unknown, ThrowOnError>({ url: '/api/v1/shows/{id}/seasons/{season_number}/episodes', ...options });

/**
 * Aggregate stats for the dashboard.
 */
export const stats = <ThrowOnError extends boolean = false>(options?: Options<StatsData, ThrowOnError>) => (options?.client ?? client).get<StatsResponses, unknown, ThrowOnError>({ url: '/api/v1/stats', ...options });

/**
 * Returns server status with health warnings (no auth required).
 */
export const getStatus = <ThrowOnError extends boolean = false>(options?: Options<GetStatusData, ThrowOnError>) => (options?.client ?? client).get<GetStatusResponses, unknown, ThrowOnError>({ url: '/api/v1/status', ...options });

/**
 * List all scheduled tasks.
 */
export const listTasks = <ThrowOnError extends boolean = false>(options?: Options<ListTasksData, ThrowOnError>) => (options?.client ?? client).get<ListTasksResponses, unknown, ThrowOnError>({ url: '/api/v1/tasks', ...options });

/**
 * Trigger a task to run immediately.
 */
export const runTask = <ThrowOnError extends boolean = false>(options: Options<RunTaskData, ThrowOnError>) => (options.client ?? client).post<RunTaskResponses, RunTaskErrors, ThrowOnError>({ url: '/api/v1/tasks/{name}/run', ...options });

/**
 * Discover movies (popular, optional genre filter).
 */
export const discoverMovies = <ThrowOnError extends boolean = false>(options?: Options<DiscoverMoviesData, ThrowOnError>) => (options?.client ?? client).get<DiscoverMoviesResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/discover/movies', ...options });

/**
 * Discover TV shows with filters.
 */
export const discoverShows = <ThrowOnError extends boolean = false>(options?: Options<DiscoverShowsData, ThrowOnError>) => (options?.client ?? client).get<DiscoverShowsResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/discover/shows', ...options });

/**
 * Get movie and TV genre lists.
 */
export const genres = <ThrowOnError extends boolean = false>(options?: Options<GenresData, ThrowOnError>) => (options?.client ?? client).get<GenresResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/genres', ...options });

/**
 * Get TMDB movie details by TMDB ID.
 */
export const movieDetails = <ThrowOnError extends boolean = false>(options: Options<MovieDetailsData, ThrowOnError>) => (options.client ?? client).get<MovieDetailsResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/movies/{tmdb_id}', ...options });

/**
 * Search TMDB for movies and TV shows.
 */
export const search = <ThrowOnError extends boolean = false>(options: Options<SearchData, ThrowOnError>) => (options.client ?? client).get<SearchResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/search', ...options });

/**
 * Get TMDB show details by TMDB ID.
 */
export const showDetails = <ThrowOnError extends boolean = false>(options: Options<ShowDetailsData, ThrowOnError>) => (options.client ?? client).get<ShowDetailsResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/shows/{tmdb_id}', ...options });

/**
 * Get TMDB season details (episodes) for a show.
 */
export const seasonDetails = <ThrowOnError extends boolean = false>(options: Options<SeasonDetailsData, ThrowOnError>) => (options.client ?? client).get<SeasonDetailsResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/shows/{tmdb_id}/seasons/{season_number}', ...options });

/**
 * Trending movies this week.
 */
export const trendingMovies = <ThrowOnError extends boolean = false>(options?: Options<TrendingMoviesData, ThrowOnError>) => (options?.client ?? client).get<TrendingMoviesResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/trending/movies', ...options });

/**
 * Trending TV shows this week.
 */
export const trendingShows = <ThrowOnError extends boolean = false>(options?: Options<TrendingShowsData, ThrowOnError>) => (options?.client ?? client).get<TrendingShowsResponses, unknown, ThrowOnError>({ url: '/api/v1/tmdb/trending/shows', ...options });

/**
 * `GET /api/v1/vpn/status` — snapshot of the in-memory tunnel state.
 */
export const getStatus2 = <ThrowOnError extends boolean = false>(options?: Options<GetStatus2Data, ThrowOnError>) => (options?.client ?? client).get<GetStatus2Responses, unknown, ThrowOnError>({ url: '/api/v1/vpn/status', ...options });

/**
 * `POST /api/v1/vpn/test` — runs the VPN health check (confirms a
 * recent handshake, reconnects if stale) and looks up the public IP
 * via api.ipify.org so the user can confirm traffic is actually
 * routing through the tunnel.
 */
export const testConnection = <ThrowOnError extends boolean = false>(options?: Options<TestConnectionData, ThrowOnError>) => (options?.client ?? client).post<TestConnectionResponses, unknown, ThrowOnError>({ url: '/api/v1/vpn/test', ...options });

/**
 * `POST /api/v1/watch-now` — see module docs.
 */
export const watchNow = <ThrowOnError extends boolean = false>(options: Options<WatchNowData, ThrowOnError>) => (options.client ?? client).post<WatchNowResponses, WatchNowErrors, ThrowOnError>({
    url: '/api/v1/watch-now',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List webhook targets.
 */
export const listWebhooks = <ThrowOnError extends boolean = false>(options?: Options<ListWebhooksData, ThrowOnError>) => (options?.client ?? client).get<ListWebhooksResponses, unknown, ThrowOnError>({ url: '/api/v1/webhooks', ...options });

/**
 * Create a webhook target.
 */
export const createWebhook = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookData, ThrowOnError>) => (options.client ?? client).post<CreateWebhookResponses, unknown, ThrowOnError>({
    url: '/api/v1/webhooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a webhook target.
 */
export const deleteWebhook = <ThrowOnError extends boolean = false>(options: Options<DeleteWebhookData, ThrowOnError>) => (options.client ?? client).delete<DeleteWebhookResponses, DeleteWebhookErrors, ThrowOnError>({ url: '/api/v1/webhooks/{id}', ...options });

/**
 * Update a webhook target.
 */
export const updateWebhook = <ThrowOnError extends boolean = false>(options: Options<UpdateWebhookData, ThrowOnError>) => (options.client ?? client).put<UpdateWebhookResponses, UpdateWebhookErrors, ThrowOnError>({
    url: '/api/v1/webhooks/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a synthetic "imported" event to a single target, bypassing
 * the enabled + per-event toggles + backoff checks. Lets users
 * verify wiring (URL, auth headers, body template) without having
 * to trigger a real grab.
 */
export const testWebhook = <ThrowOnError extends boolean = false>(options: Options<TestWebhookData, ThrowOnError>) => (options.client ?? client).post<TestWebhookResponses, TestWebhookErrors, ThrowOnError>({ url: '/api/v1/webhooks/{id}/test', ...options });

/**
 * Flat counter summary for external dashboards (Homepage etc.).
 */
export const widget = <ThrowOnError extends boolean = false>(options?: Options<WidgetData, ThrowOnError>) => (options?.client ?? client).get<WidgetResponses, unknown, ThrowOnError>({ url: '/api/v1/widget', ...options });

/**
 * WebSocket endpoint for real-time event push.
 */
export const wsHandler = <ThrowOnError extends boolean = false>(options?: Options<WsHandlerData, ThrowOnError>) => (options?.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/v1/ws', ...options });
