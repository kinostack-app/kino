# Third-party licences

This file is generated by `cargo-about` from the resolved Rust
dependency tree of the kino crate. It lists every direct +
transitive Rust dependency, the licence it ships under, and the
full licence text.

Generated: by `cargo about generate about.hbs > THIRD_PARTY_LICENSES.md`
on every release tag (CI step in `.github/workflows/release.yml`).
Also runnable locally via `just licenses`.

This file currently covers Rust dependencies only. The bundled
frontend SPA (React, TanStack Query, shadcn/ui, etc.) is built with
npm; per-npm-package licence aggregation is a future enhancement —
all npm deps are MIT or Apache-2.0 per `web/site/package.json` /
`frontend/package.json`'s direct deps + their resolved transitive
trees, but a generated artefact is not yet committed.

## Overview

{{#each overview}}
- [{{name}}](#{{id}}) — {{count}} crate{{#unless (eq count 1)}}s{{/unless}}
{{/each}}

---

{{#each licenses}}
## {{name}} {#{{id}}}

Used by:

{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{text}}
```

---

{{/each}}
